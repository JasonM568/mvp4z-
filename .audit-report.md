# 四象天機決策報告：會員端敵意稽核

> **結案狀態（PM 於 2026-09-23 補記）**
>
> 九條經 PM 逐條查證，**全部屬實，沒有一條誤報**。全部已修並上線。
>
> | # | 級別 | 結案 | commit |
> |---|---|---|---|
> | 1 | P0 | ✅ | `3d8c8ad` insert 失敗不 throw ＋ 精簡欄位重試 ＋ `run_id` |
> | 2 | P1 | ✅ | `df86083` 性別改為不預選；八字啟用時提示「沒選就不排大運」 |
> | 3 | P1 | ✅ | `df86083` schema 補 `timeMode`/`time` |
> | 4 | P1 | ✅ | `df86083` `parseMovingLine()` 中文爻位轉數字 |
> | 5 | P1 | ✅ | `df86083` 讀 `timeKnown`，連 hour/minute 一起不採信 |
> | 6 | P1 | ✅ | `4f1bc10` 大運 birthYear 改取 `resolved.civil` 的國曆年 |
> | 7 | P2 | ✅ | `f13ce2b` 移除兩個假旋鈕；奇門接上獨立時間線 |
> | 8 | P2 | ✅ | `f13ce2b` + `ccc9ae0` 兜底稿依實際盤面與啟用模組生成 |
> | 9 | P2 | ✅ | `3d8c8ad` 只有伺服器明確回錯才清 PENDING |
>
> **#1 沒有包成單一交易**：扣點 RPC 與 `council_runs` 是兩次呼叫，包交易要動
> SQL function。這次的處置是不讓失敗有能力吃掉報告——收了錢就一定要交出東西，
> 這條優先於資料完整。若日後要做真正的原子提交，本條仍是入口。
>
> **#9 的分類是不對稱的**：只有伺服器明確回錯才清 PENDING。連線中斷或 504 時
> 報告很可能真的跑完並扣了點，清掉會讓會員永遠回不到那份報告。分不出來時一律
> 當「沒聽到回音」。
>
> **#7 兩個旋鈕是移除不是接上**：「策略校核層」沒有安全的可選校核流程契約，
> 「八字判讀方式」的「補充四柱」沒有輸入欄位、「只看流年」沒有對應引擎模式。
> 留一個假的旋鈕比沒有旋鈕更糟。
>
> **稽核本身也有一條漏網的**：`scripts/test-council-structured.mjs` 用
> `JSON.stringify` 比對 API 與 DB 的 structured，但 Postgres 的 jsonb 會重排鍵，
> 兩邊內容一樣也永遠比不過。它先前是綠的只因為當時 structured 兩邊都是 null。
> 已於 `7424ab0` 改為鍵序無關比對。
>
> **驗收**：正式站實跑 council（扣 20 點），HTTP 200 / 95 秒 / 無兜底 /
> structured 正常；查正式庫確認大運 8 步順逆與干支正確、十神藏干四柱齊全、
> 六爻巽宮本宮卦世六應三本卦變卦納甲俱全。
>
> **仍待風羿老師（沒有工程解）**：決策 5/6/7/8/9 簽核、奇門三張盤例校對、
> 旺衰權重、藏干權重、神煞取用。

---

稽核日期：2026-09-23。範圍：`app/member-ai/decision/`、`app/api/ai/council/route.ts`、`lib/yixue/`、報告預設設定與必要的橋接／驗證檔。方法：唯讀追蹤前端欄位到 Zod、排盤、prompt、扣點與報告紀錄的資料流；未登入正式站、未送出付費報告。下述是程式碼可證的缺口；需特定 DB 或網路故障才觸發者另註條件。

## 發現（9 條）

### 1. P0｜扣點成功後，報告紀錄寫入失敗會造成「扣點、無報告」

- 證據：`app/api/ai/council/route.ts:269-292` 先呼叫 `commit_council_credit`；`app/api/ai/council/route.ts:296-319` 後寫 `council_runs`，失敗直接 `throw`；`app/api/ai/council/route.ts:321-332` 僅在寫入成功後回傳報告。前端 `app/member-ai/decision/page.tsx:252-264` 對錯誤只顯示掃描失敗；`app/member-ai/decision/_steps/scanning-step.tsx:94-100` 固定告知「未扣點」。
- 會員會看到什麼：若扣點 RPC 成功，但 `council_runs` insert 因 DB 短暫錯誤、欄位約束或連線問題失敗，會員會被告知「本次未產出報告、未扣點」，實際點數已扣；「我的巽風」也沒有這份報告可找回。
- 判定：程式順序確定；正式庫是否已發生，未查。需要將扣點與報告寫入綁成同一個交易，或讓失敗時有可核對的補償／找回機制。

### 2. P1｜新上線大運仍以「男」作靜默預設，未選性別也會排出確定方向

- 證據：`app/member-ai/decision/_form-config.ts:118-120` 預設 `gender: "男"`，註解已預告這個風險；`app/member-ai/decision/_steps/input-step.tsx:125-145` 按鈕直接顯示男為選中，沒有要求會員主動確認；`app/member-ai/decision/_actions.ts:108-111` 將其送出；`lib/ai/council/chart.ts:186-188` 傳給引擎；`lib/yixue/bazi/luck.ts:43-49,83-88` 依性別決定順逆及起運。
- 會員會看到什麼：只填問題與生日、沒碰性別的女性會員，會得到按男性規則排出的整組大運與「陽年男／陰年男」判定，報告還會當成系統既定事實引用（`lib/yixue/format/prompt.ts:96-106`）。這會影響運序與時間窗口。
- 判定：確定。建議預設「不指定」，不指定時不排大運，或生成前要求明確選擇。

### 3. P1｜六爻選「現在時間」，時間在 API 驗證時被丟掉

- 證據：表單承諾「以送出當下的台北時間起卦」（`app/member-ai/decision/_steps/input-step.tsx:291-307`）；payload 會送 `liuyao.timeMode`、`liuyao.time`（`app/member-ai/decision/_actions.ts:134-143`）；但 `lib/ai/council/schema.ts:57-62` 的 `liuyao` schema 僅收 `mode`、`yao`，Zod object 預設移除未宣告欄位。橋接 `lib/ai/council/chart.ts:122-124` 因而讀不到 `time`，回退 `eventTime`。
- 會員會看到什麼：若進階區「事件時間」不同於送出當下，六爻盤仍按事件時間排；會員看到自己選了「現在時間」卻拿到另一時刻的月建、日辰、六神及卦象。預設事件時間也在打開頁面時就固定（`app/member-ai/decision/_form-config.ts:114-115,137-141`），晚些送出同樣會有差異。
- 判定：確定。此處前後端接受欄位不一致。

### 4. P1｜梅花「上下卦起卦」手選動爻會被整組改成時間起卦

- 證據：手選選項是「初爻」至「上爻」（`app/member-ai/decision/_form-config.ts:54`；`app/member-ai/decision/_steps/input-step.tsx:347-352`），payload 原樣送中文標籤（`app/member-ai/decision/_actions.ts:42-48`）。橋接卻執行 `Number(m.movingLine)` 並要求 1–6（`lib/ai/council/chart.ts:145-152`）；`Number("初爻")` 為 `NaN`，故落到時間起卦。
- 會員會看到什麼：即使精心選了上卦、下卦和動爻，付費報告仍依時間另起一卦，可能完全不同；盤面會標「時間起卦」（`lib/yixue/format/prompt.ts:151-157`）。
- 判定：確定。六個中文標籤均無法通過數字判斷。

### 5. P1｜會員選「時辰不確定」，引擎仍產出時柱

- 證據：表單有獨立「時辰是否確定」選項（`app/member-ai/decision/_steps/input-step.tsx:207-209`），payload 傳 `birth.timeKnown`（`app/member-ai/decision/_actions.ts:112-123`），schema 也接受（`lib/ai/council/schema.ts:18-32`）；但橋接 `lib/ai/council/chart.ts:33-61` 沒讀 `timeKnown`，只依 `hourBranch` 是否在十二支決定；引擎 `lib/yixue/calendar/resolve.ts:80-105` 也只依 `hourBranch` 設 `hourKnown`。預設時辰為「寅」（`app/member-ai/decision/_form-config.ts:130`）。
- 會員會看到什麼：把「時辰是否確定」改為「否」或「不確定」，若未同步把出生時辰改成「不確定」，報告仍列出確定時柱、十神與大運資訊，資料完整度也不會因未知時辰降低。這是填了卻未進計算的欄位。
- 判定：確定。需讓 `timeKnown` 與 `hourBranch` 的衝突有明確處理。

### 6. P1｜農曆年底出生者的大運西元年份可能整體提前一年

- 證據：農曆日期在 `lib/yixue/calendar/resolve.ts:69-78` 轉成國曆，該日期才是實際出生年；但 `lib/yixue/index.ts:116-124` 傳入大運的 `birthYear` 仍是原表單農曆年；`lib/yixue/bazi/luck.ts:90-107` 以此加上起運歲數形成每一步的 `fromYear`／`toYear`；`lib/yixue/format/prompt.ts:100-105` 把它當「西元年為準」印給模型。
- 會員會看到什麼：例如農曆某年十二月出生、實際國曆生日落在次年一月者，報告的大運起迄西元年會比實際早一年，「事件時刻所在大運」也可能在交界年標錯。
- 判定：程式推導確定；未逐例跑曆法 golden case。應改用轉換後的國曆出生年及精確起運日期驗算。

### 7. P2｜三個進階選項看似控制判讀，實際不控制引擎或校核

- 證據：表單提供「策略校核層」（`app/member-ai/decision/_steps/input-step.tsx:210`）與八字「判讀方式」（`:276-279`），兩者存於 `CouncilForm`（`app/member-ai/decision/_form-config.ts:80,86`），但 `buildCouncilPayload`（`app/member-ai/decision/_actions.ts:101-149`）完全未送出。奇門「起局方式」會送出並顯示於 prompt（`_actions.ts:133`；`lib/ai/council/personas.ts:153-156`），但 `lib/ai/council/chart.ts:181-193` 沒傳給引擎，`lib/yixue/index.ts:171-181` 只按共用 `divinationTime` 起局。
- 會員會看到什麼：選「不啟用」或「啟用深度反證層」，模型流程仍照常進第二輪，與選「策略校核層」無差；選「只看流年趨勢」仍會得到完整四柱／大運；奇門選「指定時間」或「現在起局」可能同樣使用梅花自填時間或事件時間，顯示的選項與盤的時間來源不一致。
- 判定：確定。這些是介面承諾與實際行為不符，需移除無效選項或明確接線。

### 8. P2｜兜底交付稿仍宣稱「大運未納入」，且固定列未啟用術數

- 證據：正式盤已輸出大運（`lib/yixue/format/prompt.ts:89-106`），但 `lib/ai/council/settings/defaults.ts:263,270-275` 的兜底文字仍說「大運未納入／尚未納入」，並要求補已由引擎算出的世應、動爻、變卦、梅花互卦等；`lib/ai/council/settings/render.ts:195-211` 固定按四術產文，不看會員啟用模組。API 終稿不合門檻時使用此稿（`app/api/ai/council/route.ts:202-205,237-247`）。
- 會員會看到什麼：即使有出生資料與系統盤，兜底報告仍說大運不存在、叫會員補卦象；只啟用一術也會收到四術內容。此路徑不扣點，但報告被標為成功交付（`app/api/ai/council/route.ts:322-329`），會削弱信任。
- 判定：確定。觸發條件是終稿未通過 `hasUsableFinal`。

### 9. P2｜請求失敗後重新整理，會再卡進最多五分鐘的「找回中」

- 證據：送出前寫 `PENDING_KEY`（`app/member-ai/decision/page.tsx:242-250`）；API 明確回錯或 fetch 失敗時只設定錯誤並結束（`:252-264`），沒有移除 pending。重新整理時 `:70-89` 看到六分鐘內 pending 便進掃描，`:92-126` 每五秒查一次、最多六十次才顯示找不到。錯誤畫面固定說「未扣點」（`app/member-ai/decision/_steps/scanning-step.tsx:94-100`）。
- 會員會看到什麼：例如點數不足或輸入驗證失敗後刷新頁面，明明沒有在生成，卻看到「正在找回先前進行中的四象合參」並等約五分鐘，最後只能回表單；若是網路在後端成功後中斷，畫面又可能先誤稱未扣點。
- 判定：確定。需分辨「已明確拒絕」與「結果未知」兩種錯誤，前者清 pending，後者保留並立即查找。

## 未列為缺陷的核對

- 旺衰：盤面提供十神、藏干與五行分佈，但未提供旺衰分數，`lib/yixue/format/prompt.ts:67-69` 已明說權重待老師定；目前沒有足夠依據把「未提供旺衰分數」本身判成實作錯誤。
- 應期：預設骨架 `lib/ai/council/settings/defaults.ts:214-217` 明確要求無法精定時承認不足，故不把每份報告沒有精確日期列為 bug。
- 後台設定：`lib/ai/council/settings/load.ts:34-51` 有 60 秒快取，`:83-99` 設定驗證失敗時回退預設；此為可觀察風險，但本次未讀正式 DB／後台畫面，不能聲稱有某筆發布後未生效。已確定的「顯示與實際不一致」在第 7 條會員端選項。

## 建議處理順序

1. 先處理第 1 條：把扣點與 `council_runs` 持久化做成原子提交，並核對是否已有扣點但無報告的會員交易。
2. 再處理第 2–6 條：它們會改變付費報告的盤面或年份。每條用一個能區分前後結果的實際案例驗收。
3. 最後處理第 7–9 條：移除或接上無效選項、更新兜底稿、修正 pending 找回狀態。

本稽核未修改產品程式、未呼叫正式付費 API、未 commit。
